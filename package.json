{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "pre-release": "changeset pre enter alpha && changeset",
    "release": "turbo run build --filter=!docs && changeset"
  },
  "devDependencies": {
    "@changesets/cli": "^2.25.2"
  },
  "dependencies": {
    "turbo": "^1.6.3"
  }
}
