{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "changeset": "changeset",
    "exit-prerelease": "changeset pre exit && changeset",
    "prerelease": "changeset pre enter alpha && changeset",
    "release": "turbo run build --filter=!docs && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.25.2"
  },
  "dependencies": {
    "turbo": "^1.6.3"
  }
}
